# Local Development - API Admin New
# Overrides config.yaml for local development

server:
  port: 8081

database:
  postgres:
    host: "localhost"
    password: "edugo123"
    ssl_mode: "disable"

logging:
  level: "debug"
  format: "text"

auth:
  jwt:
    secret: "dev-secret-key-change-in-production"

cors:
  allowed_origins: "*"
